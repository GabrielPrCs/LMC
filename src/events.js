const MODEL = {
    SYNC: 'sync',
    SAVED: 'saved',
    FETCHED: 'fetche',
    DELETED: 'deleted',
    ROLLBACK: 'rollback',
};

const COLLECTION = {

};

module.exports = {
    MODEL,
    COLLECTION
};