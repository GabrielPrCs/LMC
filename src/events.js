const MODEL = {
    SYNC: 'sync',
    SAVED: 'saved',
    FETCHED: 'fetche',
    DELETED: 'deleted',
    ROLLBACK: 'rollback',
};

const COLLECTION = {

};

const PAGINATED_COLLECTION = {
    
};

module.exports = {
    MODEL,
    COLLECTION,
    PAGINATED_COLLECTION,
};